# Configuration Evidence

Screenshots are organized by infrastructure component. Evidence should show the verified resulting configuration, not only the commands or clicks used to create it. Redact credentials, keys, tokens, public addresses, serial numbers and unique identifiers before publication.
